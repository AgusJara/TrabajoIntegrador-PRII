<!DOCTYPE html>
<html lang="es">
<!-- Include Head -->
<%- include('./partials/head') %>


<body>
	<!-- Header -->
	<% if (locals.usuarioLogueado != undefined) { %>
		<%- include('./partials/headerLogueado')%>
	<% } else { %>
		<%- include('./partials/header')%>
	<% } %>
	<!-- /Header -->

	<!-- Novedades -->
	<div class="container products-wrapper">
		<div class="row">
			<div class="col-12">
				<% if (productos == 0) { %>
					<h1>No hay resultados de búsqueda para '<%= busqueda %>'.</h1>
				<% } else { %>
					<h2 class="products-title"> Resultados de búsqueda </h2>
					<ul>
			<% for(i=0; i<productos.length; i++) { %>
				<div class="col-12 col-sm-6 col-lg-3">
					<section class="product-box">
						<a href="/productos/detalle/<%= productos[i].id %>">
							<figure class="product-box_image">
								<img src="/images/products/<%= productos[i].imagen %>" alt="<%= productos[i].nombre %>">
							</figure>
							<article class="product-box_data">
								<h2> <%= productos[i].nombre %> </h2>
								<p> <%= productos[i].descripcion %> </p>
							    <p>Comentarios: <%= productos[i].producto_comentario.length %> </p> 
							</article>
						</a>
						<p class="usuario"> Publicado por: <a href="/users/profile/<%=productos[i].producto_usuario.id%>"> <%= productos[i].producto_usuario.usuario %>
						</a>
						</p>
					</section>
				</div>
			<% } %>
			</ul>
		<% } %>
			</div>

			
			<!-- Show the "ultimos" products -->
			

	<!-- footer -->
	<%- include('./partials/footer')%>
	<!-- /footer -->
</body>

</html>